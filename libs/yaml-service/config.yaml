server:
  host: "localhost"
  port: 8080
  enable_ssl: true

database:
  credentials:
    user: "root"
    password: "pass123"
  max_connections: 10

numbers:
  list_of_ints: [1, 2, 3, 4, 5]
  list_of_floats: [1.0, 2.5, 3.14, 4.2]

message: "Hello from YAML!"
